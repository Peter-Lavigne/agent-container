[project]
name = "agent-container"
version = "1.0.0"
dependencies = [
    "pl-ci-cd>=0.0.78",
]
requires-python = ">=3.12"

[tool.uv.sources]
pl-ci-cd = { path = "/home/peter/src/pl-ci-cd", editable = true }
